/*
#include<stdio.h>

float max(float x,float y)
{
    return (x>y? x:y);
}

int main()
{
    float num[3],m;
    int i;
    scanf("%f%f%f",&num[0],&num[1],&num[2]);
    for(i=1,m=num[0];i<3;i++)
    {
        if(m<max(m,num[i]))
        {
            m=max(m,num[i]);
        }
    }
    printf("max=%.2f",m);
    return 0;
}
*/